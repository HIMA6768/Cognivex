# Safe R1 application settings for local Streamlit development.
COGNIVEX_ENVIRONMENT=development
COGNIVEX_DEBUG=true

# Dataset ingestion, schema mappings, model paths, subtype labels, and
# evaluation settings are intentionally absent until their implementing phase.
