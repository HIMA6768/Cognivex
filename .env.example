# Safe R1 application settings for local Streamlit development.
COGNIVEX_ENVIRONMENT=development
COGNIVEX_DEBUG=true

# Canonical R2 data uses repository-relative paths under data/metabric/ and
# needs no environment variable or external ZIP path. Model settings remain deferred.
